type TypeRoutes = {
  root: 'http://localhost:3000/api';
  appeals: '/appeals';
  searchAppeal: '/search-appeal';
};

const routes: TypeRoutes = {
  root: 'http://localhost:3000/api',
  appeals: '/appeals',
  searchAppeal: '/search-appeal',
};

export default routes;
